<div class="signature">
    @if (! empty($signatureImagePath))
        <img src="{{ $signatureImagePath }}" class="signature-image" alt="Signature">
    @endif
    <div class="signature-line">Authorized Signature</div>
</div>
